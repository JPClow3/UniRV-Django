# ============================================
# AgroHub - Docker Environment Variables
# ============================================
# Copy this file to .env before running docker-compose
#
# Usage:
#   cp .env.docker .env
#   # Edit .env with your production values
#   docker-compose up --build -d
#
# NEVER commit .env to version control!
# ============================================

# ============================================
# REQUIRED - Must be set before deployment
# ============================================

# Django secret key (generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
SECRET_KEY=change-me-to-a-secure-random-string

# Debug mode - MUST be False in production
DJANGO_DEBUG=False

# Allowed hosts - your domain(s), comma-separated
# Example: ALLOWED_HOSTS=agrohub.unirv.edu.br,www.agrohub.unirv.edu.br
ALLOWED_HOSTS=localhost

# Database password (use a strong password!)
DB_PASSWORD=change-me-to-a-secure-password

# ============================================
# DATABASE (PostgreSQL)
# ============================================
# These use Docker container hostnames by default

DB_NAME=agrohub_production
DB_USER=agrohub_user
# DB_HOST and DB_PORT are set automatically in docker-compose.yml
# DB_HOST=db
# DB_PORT=5432

# ============================================
# REDIS CACHE
# ============================================
# Redis hostname is set automatically in docker-compose.yml
# REDIS_HOST=redis
# REDIS_PORT=6379

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
# If not configured, emails will be printed to console

# For production SMTP:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=noreply@yourdomain.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# For development (console output):
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@agrohub.unirv.edu.br

# ============================================
# APPLICATION SETTINGS
# ============================================

# Site URL for email links and absolute URLs
SITE_URL=http://localhost

# Logging
DJANGO_LOG_LEVEL=INFO
DJANGO_LOG_TO_FILE=true

# ============================================
# GUNICORN SETTINGS
# ============================================

# Number of Gunicorn workers (leave empty for auto: 2*CPU+1)
# For small VPS (1-2 CPU): 3-5 workers recommended
# GUNICORN_WORKERS=3

# ============================================
# STATIC FILES & COMPRESSION
# ============================================

# Enable CSS/JS compression
COMPRESS_ENABLED=true

# Cache max age for static files (1 year = 31536000)
WHITENOISE_MAX_AGE=31536000

# ============================================
# OPTIONAL ADVANCED SETTINGS
# ============================================

# Cookie domain for subdomain support
# COOKIE_DOMAIN=.yourdomain.com

# CDN for images (Cloudflare Images, Cloudinary, etc.)
# CDN_BASE_URL=https://imagedelivery.net/your-account-id

# Partners for home page marquee
# PARTNERS=SENAI,SEBRAE,FAPEG,FINEP,UNIRV,INOVALAB

# ============================================
# NOTES
# ============================================
#
# 1. Generate a secure SECRET_KEY:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# 2. Generate a secure DB_PASSWORD:
#    openssl rand -base64 32
#
# 3. For SSL/HTTPS:
#    - Place certificates in docker/nginx/ssl/
#    - Update docker/nginx/conf.d/default.conf
#    - Change SITE_URL to https://
#
# 4. For Let's Encrypt:
#    certbot certonly --webroot -w /var/www/certbot -d yourdomain.com
#
# 5. After changing environment variables:
#    docker-compose down && docker-compose up -d
