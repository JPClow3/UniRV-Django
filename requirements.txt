# Core Django and web server
Django>=5.2.8,<6.0  # Latest stable Django 5.x LTS
whitenoise>=6.11.0,<7.0  # Static file serving with compression
gunicorn>=23.0.0,<26.0  # Production WSGI server
# uvicorn>=0.38.0  # Optional: ASGI server for WebSockets/async support (not currently used - project uses WSGI with gunicorn)

# Static compression (Brotli) for WhiteNoise
# Enables precompressed .br assets when using WhiteNoise Compressed* storage backends.
Brotli>=1.1.0,<2.0  # Best compression ratio for static assets

# Content processing
bleach>=6.3.0,<7.0

# Django extensions
django-tailwind>=3.8.0,<5.0  # Tailwind CSS integration (v4 skipped due to breaking changes)
django-browser-reload>=1.21.0,<2.0  # Auto-reload browser during development
django-redis>=6.0.0,<7.0  # Redis cache backend
django-widget-tweaks>=1.5.1,<2.0  # Template filters for form rendering

# Image processing (required for ImageField)
Pillow>=11.0.0,<13.0
easy-thumbnails>=2.10.1,<3.0

# Database (production)
psycopg2-binary>=2.9.10,<3.0  # PostgreSQL adapter

# Cache backend (used as fallback in settings)
redis>=5.0.0,<8.0

# Audit logging
django-simple-history>=3.11.0,<4.0

# Environment management
django-environ>=0.12.0,<1.0

# Async task queue
celery[redis]>=5.4.0,<6.0
django-celery-beat>=2.7.0,<3.0
flower>=2.0.0,<3.0

# Health checks
django-health-check>=3.18.0,<4.0

# Autocomplete widgets
django-autocomplete-light>=3.11.0,<4.0

# Performance profiling (conditionally enabled)
django-silk>=5.3.0,<6.0

# Email service provider integration (MailerSend)
django-anymail[mailersend]>=12.0,<13.0

# Auto-delete orphaned files on model update/delete
django-cleanup>=9.0.0,<10.0

# Note: Regularly update dependencies for security patches
# Use 'pip list --outdated' to check for updates
# Review changelogs before updating major versions
#
# Development/testing dependencies are in requirements-dev.txt
