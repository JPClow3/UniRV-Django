# Core Django and web server
Django>=5.2.8
whitenoise>=6.11.0
gunicorn>=23.0.0
# uvicorn>=0.38.0  # Optional: ASGI server for WebSockets/async support (not currently used - project uses WSGI with gunicorn)

# Content processing
bleach>=6.3.0

# Django extensions
django-compressor>=4.6.0
django-tailwind>=3.8.0
django-browser-reload>=1.11.0
django-redis>=6.0.0
django-ratelimit>=4.1.0
django-widget-tweaks>=1.5.0

# Image processing (required for ImageField)
Pillow>=11.0.0
easy-thumbnails>=2.8

# Database (production)
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# Cache backend (used as fallback in settings)
redis>=5.0.0

# Security auditing tool (for CI/CD and security checks, not a runtime dependency)
# Run with: pip-audit -r requirements.txt
# See: https://pypi.org/project/pip-audit/
pip-audit>=2.6.0

# Audit logging
django-simple-history>=3.4.0

# Note: Regularly update dependencies for security patches
# Use 'pip list --outdated' to check for updates
# Review changelogs before updating major versions
